{
  "name": "AI CRM",
  "dockerComposeFile": ["../docker-compose.local.yml"],
  "service": "api",
  "workspaceFolder": "/app",
  "forwardPorts": [3000, 8000],
  "postCreateCommand": "pnpm install || true"
}
