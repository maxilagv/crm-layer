Respondé SOLO con JSON válido según el schema. Si no hay tareas, devolvé una lista vacía. Toda tarea dudosa lleva "requires_confirmation": true.

## Template
Contacto:
{contact_profile}

Mensajes cercanos:
{recent_messages}

Mensaje actual:
{current_message}
