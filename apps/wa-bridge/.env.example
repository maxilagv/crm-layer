# Copiá este archivo a `.env` y completá ORGANIZATION_ID.
BACKEND_URL=http://localhost:8000
ORGANIZATION_ID=
# Debe coincidir con WA_BRIDGE_SHARED_SECRET del backend (.env de apps/api).
WA_BRIDGE_SHARED_SECRET=dev-bridge-secret
